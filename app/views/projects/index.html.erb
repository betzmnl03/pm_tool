<h1> All projects</h1>

<% @projects.each do |project|%>
<div class="card mb-4">
<div class="card-body">
<h3><%= link_to(project.title, project_path(project.id))%></h3>
<p><%= project.description %></p>
<p>Created_by: <%= project.user&.full_name %></p>
</div>
</div>
<%end%>


